export const archiveRegExp = /\.(zip|tar\.gz|7z|s7z|rar|tgz|gz)$/i;
export const imageRegExp = /\.(gif|jpg|jpeg|tiff|png|webp|svg)$/i;
export const videoRegExp = /\.(mp4|avi|mkv|webm|ogv|mov|wmv)$/i;

export default {
  archiveRegExp,
  imageRegExp,
  videoRegExp,
};
