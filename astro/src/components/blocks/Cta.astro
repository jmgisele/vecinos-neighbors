---
import Section from '../ui/Section.astro';
import Button from '../ui/Button.astro';
import Icon from '../ui/Icon.astro';
import { tinaField } from '@tinacms/astro/tina-field';
import OrbitRings from '../space/OrbitRings.astro';
import type { CtaBlock } from '../../lib/data';

interface Props { data: CtaBlock }

const { data } = Astro.props;
const actions = (data.actions ?? []).filter(a => a !== null);
---
<Section class="relative overflow-hidden">
	<OrbitRings class="opacity-70" />
	<div class="relative text-center">
		<h2 class="text-balance text-4xl font-semibold lg:text-5xl" >{data.title}</h2>
		<p class="mt-4 text-muted-foreground" >{data.description}</p>
		<div class="mt-12 flex flex-wrap justify-center gap-4">
			{actions.map((action) => (
				<div class="bg-foreground/10 rounded-2xl border p-0.5" >
					<Button href={action.link} size="lg" variant={action.type === 'link' ? 'accent' : 'default'} class="rounded-xl px-5 text-base">
						{action.icon && <Icon name={action.icon} class="size-4" />}
						<span class="text-nowrap">{action.label}</span>
					</Button>
				</div>
			))}
		</div>
	</div>
</Section>
